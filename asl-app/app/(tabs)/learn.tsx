import { Text, View } from 'react-native';

export default function LearnScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-surface">
      <Text className="text-2xl font-bold text-slate-800">Apprendre</Text>
    </View>
  );
}
